我想效仿希望之村的方式来实现在屏幕上以网格的形式来放置物品的形式，包括网格拖拽、吸附等功能，但我想摆放的不是建筑或物品，而是打卡图标、文字框、贴纸等，我想做的是一个手账软件，我不需要希望之村一样无限的地形图，网格只需要铺满一整个屏幕就可以，我希望在所有画布元素左上角添加一个删除按钮；

我希望在画布右上角添加一个保存按钮，点击保存后可以填写画布标题，填写完成之后将当前画布内容保存在手账本中；

tab栏：在屏幕下方添加tab栏，左边是首页，右边是手账本，中间是一个用来添加画布元素的加号按钮，点击进入编辑模式，切换tab栏为工具栏，工具栏中有打卡、文字框、贴纸等按钮，点击可以在屏幕上添加对应画布元素，编辑模式下画布元素才可以被拖拽，我希望在编辑模式下画布右上角可以添加一个对号按钮，点击表示编辑完成，编辑完成后切换工具栏为tab栏，画布元素不可以再进行拖拽；

手账本：用来保存所有被保存的画布内容，未打开画布内容之前只显示画布标题，打开后查看相应画布内容；

首页：从手账本跳转回首页后我希望首页原来正在编辑的内容保持不变；

我希望在首页可以添加多个临时画布，每个画布还可以填写标题，画布之间可以切换；

打卡：我希望可以效仿小日常等打卡软件，添加图标之前可以填写打卡名称，选择打卡完成颜色，打卡前图标为灰色，打卡后变为自选颜色，再次点击弹出选择弹框是否取消今日打卡，我希望图标为圆形，打卡名称位于图标之下，拖拽时图标可以和打卡名称一起移动；

文字框：我希望文字框可以贴合网格边框，拖拽文字框右下角可以改变文字框大小，但 文字款左上角位置不变，拖拽文字框其他位置可以改变文字框整体位置，文字可以随文字框大小的变化而换行；

我希望可以有一个更新按钮，点击后打卡就会更新为未打卡状态，文字框中的文字也更新为未编辑状态；

我希望在编辑模式下画布右下角可以有一个加号按钮和一个减号按钮，点击加号按钮可以在原本的画布下面添加三分之一屏的画布长度，点击减号按钮则减少三分之一屏的画布长度，但是画布最少得有一屏幕的长度；

先给我做一个简单的纯前端网页原型，我要做成html文件。

这个效果就可以，可以用 Vue3+Vite+Capacitor+pinia 帮我做一下吗，包括创建流程，代码，文件等，后端我想用express，数据库用mysql，再帮我添加一个用户模块，拓展一下图片贴纸上传功能，还有移动端触摸拖拽，我希望点击手账本中已保存的画布展示时可以跳转到新的页面展示而不是首页展示，首页有我原本的未保存的画布元素，我不希望这些未保存的画布元素被覆盖，从手账本打开的已保存的手账页面需要有一个编辑按钮，点击可以进入编辑模式，下方同样有工具栏；首页再增加一个用户名和退出登录按钮；另外，App.js中不要有太多代码，尽量帮我写到components中；
最后再帮我做一个后台管理平台；

## ✨项目要点回顾

1. **隔离状态：首页临时画布保存在 `homeTempCanvasStore`；打开已保存手账走独立路由`/notebook-view/:id`，使用`notebookEditStore`；首页临时画布完全不会被覆盖丢失。**
2. 画布组件`CanvasGrid.vue`同时实现 PC 鼠标拖拽 + 移动端 touch 触摸拖拽，32px 网格吸附。
3. 用户登录模块；顶部 UserHeaderBar 组件展示用户名、退出登录。
4. 支持上传图片贴纸，上传请求提交后端 Express，文件保存后端 public/upload；数据库记录贴纸资源。
5. 底部工具栏复用`BottomEditToolbar.vue`，首页编辑模式、手账详情编辑模式共用同一工具栏。
6. App.vue 保持极简仅保留 router‑view；所有业务全部拆分到 pages 与 components 目录。
7. Capacitor 可以直接打包 Android App；前端开发环境通过 vite 代理访问后端 3001 接口；打包之后需要修改 axios 请求地址为后端公网 IP。


后续已增加：

1. **首页临时画布持久化：**
重启 App，之前创建的临时画布、画布内打卡项、文字框、贴纸全部保留；保存在浏览器 localStorage；清除浏览器缓存会丢失。正式保存到手账本依旧提交 MySQL。首页与已保存手账状态依旧互相隔离，互不干扰。
2. **文字框缩放：PC 鼠标右下角手柄拖拽缩放；手机移动端手指触摸右下角手柄同样支持缩放，已经放大触摸热区方便手指操作。**
3. 已保存手账页面缩放修改之后，点击完成编辑，会把缩放后的宽高一起保存回 MySQL。


1. **已保存手账（数据库）删除：列表页删除 / 详情页顶部删除，二次弹窗确认；删除之后数据库记录清除，不可恢复。**
2. **首页多块临时画布（localStorage）Tab 上面带关闭按钮；禁止删除仅剩的最后一块画布；删除后自动切换剩余画布并更新本地存储。**
3. Capacitor 打包 apk 后 localStorage 删除逻辑在手机端同样正常运行。


